package com.ga.java.project01;

public class Authenticator {

    public User login(String user_id, String password){
        User logged_user = new User();

        return logged_user;
    }

    public boolean authenticate(String user_id, String password){
        // check if the password is correct for this user
        // outputs: password correct, password incorrect, user doesn't exist
        return true;
    }
}
