package com.ga.java.project01;

public class Account {
    // account id, balance, card type id
    // created through account manager

    public static class Action {
        // actions that will be mapped with user input
        // will act as proxy only mapping input with existing functionality
        // the purpose is to improve modularity and isolate the user from the internal functionality

    }
}
